# Step: 5-run-tests / iteration 1

BLOCKER: cargo test has not been run since switching to git dep. Need to confirm no regressions and check whether model-dependent e2e tests ran or were silently skipped.

HYPOTHESIS: All unit/integration tests should pass. The e2e_wav and e2e_transcript tests will skip if models/stt-1b-en_fr-q4.gguf and models/mimi.safetensors are absent. Need to detect this.

PLANNED FIX: Run cargo test --features "wgpu" -- --test-threads=1, capture full output, check for failures and for silent skips on model-dependent tests.

OUTCOME: INCONCLUSIVE — cargo test exits 0, but 14 of 15 tests silently skipped (return early with println! when model files absent; Cargo counts these as ok). Only 4 synthetic Q4 matmul tests with random weights genuinely ran. Missing: models/stt-1b-en_fr-q4.gguf, models/mimi.safetensors, models/tokenizer.model, web/test-loona.wav, web/test-bria.wav. Per Task 5 convergence criteria: NOT marking [x] — hollow pass. Task 5b is also blocked for same reason. Discovery task added to queue.
