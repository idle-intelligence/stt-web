# Step: 2-merge-to-main / iteration 1

BLOCKER: feat/optional-encoder has the decode path committed (1d4f530) but has not been merged into mimi-rs main. stt-web currently depends on a path dep pointing at the mimi-rs repo, and the Cargo.toml comment says to switch to git dep after the PR merges.

HYPOTHESIS: Fast-forward or merge commit of feat/optional-encoder → main is all that's needed. No conflicts expected since feat/optional-encoder was branched from main and contains only additive changes.

PLANNED FIX: In mimi-rs repo, checkout main, merge feat/optional-encoder, verify HEAD contains the decode-path commit.

OUTCOME: Success. Fast-forward merge — mimi-rs main is now at 1d4f530 (the decode-path commit). feat/optional-encoder confirmed in --merged main. Task [x].
