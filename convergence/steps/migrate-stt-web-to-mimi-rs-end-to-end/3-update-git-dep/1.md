# Step: 3-update-git-dep / iteration 1

BLOCKER: stt-web/Cargo.toml still uses `path = "../../../mimi-rs"` for the mimi-rs dep. The Cargo.toml has a comment saying to switch to git dep after the mimi-rs PR merges — which it now has (main @ 1d4f530).

HYPOTHESIS: Replacing with `git = "file:///Users/tc/Code/idle-intelligence/mimi-rs", branch = "main"` will make Cargo resolve from the git object store, picking up the merged decode path. Native build and clippy should pass since no API changed on the encode side.

PLANNED FIX: Edit Cargo.toml (and any workspace members referencing mimi-rs), run cargo build + cargo clippy, commit.
OUTCOME: Success. crates/stt-wasm/Cargo.toml updated — path dep replaced with git dep (file:///Users/tc/Code/idle-intelligence/mimi-rs, branch=main). cargo build --release --features wgpu,cli exited 0 (resolved mimi-rs v0.1.0 @ 1d4f530). cargo clippy -D warnings exited 0. Commit 175dcb0. Task [x].
