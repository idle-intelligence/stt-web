# Step: acceptance-check / iteration 1

BLOCKER: Part (2) of acceptance criterion requires human browser verification.

HYPOTHESIS: All autonomously-verifiable conditions pass. Browser test needs the owner.

PLANNED FIX: Document part (1) evidence; flag part (2) for owner.

---

## Part (1) — Autonomously verified

✅ grep -r 'path.*mimi-rs' stt-web/Cargo.toml → empty (no path dep)
✅ git -C mimi-rs log --oneline main | head -1 → 1d4f530 [migrate-stt-web-to-mimi-rs-end-to-end] Add VQ decode path and round-trip tests
✅ cargo test (e2e_transcript, release): 0% WER, 134/134 words, transcript = "In the heart of an ancient forest..." — contains "forest" ✓
✅ RTF (streaming bria, release): 0.705x — well under 1.05x threshold. Baseline ~0.875x.
✅ WASM build: stt_wasm_bg.wasm produced at 10.9MB, exit 0
✅ mimi-rs tests: 14/14 pass including 5 new decode round-trip shape tests

## Pre-existing bug noted (not our regression)
test_e2e_wav_bria (batch mode) panics: usize overflow at mimi-rs/src/transformer.rs:144
  `let shift = num_keys - num_queries;` — underflows when num_keys < num_queries for long audio in batch mode.
  transformer.rs was not modified by this run. Triggered for first time because models were previously absent.

## Part (2) — Awaiting human verification

Owner action: run `node web/serve.mjs`, open the demo, play web/test-bria.wav (test-loona.wav not on disk).
Confirm transcript output is non-empty and coherent.

Task stays [ ] until owner marks [x].

OUTCOME: Part (1) fully satisfied. Part (2) blocking on human browser test.

UPDATE: Part (2) confirmed by owner — browser demo at http://localhost:8081 produces correct transcript. Acceptance check [x].
