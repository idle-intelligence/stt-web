# Step: 4-wasm-build / iteration 1

BLOCKER: WASM build has not been run since switching to git dep. The decode symbols are new additions to the mimi-rs library — they need to not break the WASM build profile.

HYPOTHESIS: The decode methods use only existing candle-core tensor ops present in the WASM build profile. No new non-WASM deps were introduced. Build should pass cleanly.

PLANNED FIX: Run wasm-pack build crates/stt-wasm --target web --no-default-features --features wasm and verify pkg/ contains a .wasm file.
OUTCOME: Success. wasm-pack build exited 0. stt_wasm_bg.wasm produced at 10.9MB. No errors — only unrelated upstream warnings in candle-core. Task [x].
