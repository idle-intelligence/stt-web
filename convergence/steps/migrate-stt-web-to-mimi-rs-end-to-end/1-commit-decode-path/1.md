# Step: 1-commit-decode-path / iteration 1

BLOCKER: ~119 uncommitted lines completing the mimi-rs decode path (VectorQuantizer::decode, ResidualVectorQuantizer::decode_n, SplitResidualVectorQuantizer::decode, MimiModel::dequantize_codes/decode_from_codes/decode_from_codes_n) exist on feat/optional-encoder but are not committed. No round-trip tests exist for the decode API.

HYPOTHESIS: The decode path was drafted but never committed. Without a commit + tests, the branch cannot be merged to main, and stt-web cannot depend on a clean mimi-rs main.

PLANNED FIX: Review the ~119 uncommitted lines in /Users/tc/Code/idle-intelligence/mimi-rs, add round-trip unit tests (encode→decode→shape+approximate reconstruction check), run cargo clippy, and commit on feat/optional-encoder.

OUTCOME: Success. Sub-agent committed 119 lines (VQ/RVQ/SplitRVQ decode + MimiModel high-level API) with 5 new round-trip shape tests. 14/14 tests pass. Clippy clean. Commit 1d4f530 on feat/optional-encoder in mimi-rs. Task [x].
